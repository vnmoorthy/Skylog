node_modules
.next
.env
.env.local
*.log
dist
.DS_Store
coverage
.vite
# Real aircraft/airport snapshots produced by `pnpm build:data` are multi-
# megabyte and rebuilt from public sources. Keep the tiny stubs in git so
# the app works out of the box; ignore real builds via filename marker.
public/data/*.large.json.gz
